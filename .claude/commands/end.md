---
description: Update CLAUDE.md with any changes to core components or architecture discussed in the conversation
---

You are about to update the CLAUDE.md file based on the conversation history.

**Your task:**

1. Review the entire conversation history since the last time CLAUDE.md was updated
2. Identify any changes, updates, or new information about:
   - Core components mentioned in CLAUDE.md
   - Architecture patterns (MVC, microservices, layered architecture, etc.)
   - Development workflows
   - State management (Redux, Context API, Zustand, etc.)
   - Routing patterns
   - Database schemas or ORM configurations
   - API endpoints and REST/GraphQL patterns
   - Authentication/authorization systems (JWT, OAuth, sessions, etc.)
   - Middleware (Express, Next.js, etc.)
   - IPC handlers (Electron-specific)
   - Message queues or event systems
   - Caching strategies
   - Component structure and composition patterns
   - Build configuration and deployment processes
   - Environment configuration patterns
   - Testing strategies
   - Error handling patterns
   - Data fetching patterns (React Query, SWR, etc.)
   - Important development patterns or conventions

3. Read the current CLAUDE.md file
4. Update ONLY the relevant sections that need changes based on the conversation
5. Do NOT add information about trivial changes or minor bug fixes
6. Do NOT add redundant information that's already well-covered
7. Maintain the existing structure and style of CLAUDE.md
8. Focus on "big picture" architectural changes that future Claude instances need to know

**Rules:**
- Only update if there are meaningful architectural or component changes
- If nothing significant was discussed, inform the user that CLAUDE.md is already up to date
- Preserve all existing information unless it's outdated
- Keep the file concise and focused on essential knowledge

After updating (or confirming no update needed), provide a brief summary of what changed or why no update was necessary.
